apiVersion: v1
kind: Service
metadata:
  name: plarail2021-soft-backend-external
  namespace: plarail2021
spec:
  type: NodePort
  selector:
    app: plarail2021-soft-backend-external
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
