apiVersion: apps/v1
kind: Deployment
metadata:
  name: webrtc-sender
  namespace: plarail2021
spec:
  selector:
    matchLabels:
      app: webrtc-sender
  template:
    metadata:
      labels:
        app: webrtc-sender
    spec:
      containers:
        - name: webrtc-sender
          image: ghcr.io/ueckoken/plarail2021-soft-webrtc-sender:79469d0-1636781138 # {"$imagepolicy": "plarail2021:frontend-python"}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
